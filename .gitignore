# deps + env
node_modules/
.env
.env.local

# secrets people paste in by accident
*.key
ov_secret_*

# logs / local hook output
*.log
*.jsonl
logs/

# editor + OS noise
.DS_Store
*.swp
.idea/
.vscode/

# hook installs
hooks/*.bak
# generated libraries — rebuilt from src/ by install-hook.mjs (root: `npm run install:hook`,
# package: `npm run prepack`). Never committed, never edited by hand.
hooks/jev-hook-lib.cjs
packages/cline-plugin/hooks/jev-hook-lib.cjs
